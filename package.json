{
  "name": "splitpay",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "verify": "npm run typecheck",
    "ci": "npm run verify",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "echo \"Vitest arrives in S2\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
