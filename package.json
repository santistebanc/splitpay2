{
  "name": "splitpay",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "verify": "npm run typecheck && npm run format:check && npm run test && npm run test:e2e",
    "ci": "npm run verify",
    "web": "npm run web --workspace=@splitpay/mobile",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:clean": "bash scripts/clean-test-dbs.sh",
    "test:e2e": "playwright test",
    "supabase:reset": "supabase db reset",
    "powersync:start": "bash scripts/start-powersync.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  }
}
